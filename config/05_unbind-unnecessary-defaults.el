;; C-z defaults to suspend-frame which behaves weirdly and is never necessary
(unbind-key "C-z")

;; <f1> defaults to duplicate the functionality provided by C-h
;; this opens up C-h to be used for better things
(unbind-key "C-h")
